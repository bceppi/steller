<%= form_with(model: post_comment, local: true) do |form| %>
  <% if post_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_comment.errors.count, "error") %> prohibited this post_comment from being saved:</h2>

      <ul>
      <% post_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :post_comment_user_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id, id: :post_comment_post_id %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, id: :post_comment_comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
